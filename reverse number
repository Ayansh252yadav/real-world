#include<stdio.h>
int reverse(int x){
 int rem,rev=0;
 while(n!=0)
 {
    rem=x%10;
    rev=rev*10+rem;
    x=x/10;
 }
 return rev;
}
int main()
{
  int n;
  printf("enter the number to reverse :");
  scanf("%d",&n);
  printf("reverse of a number is=%d",reverse(n));
  return 0;
  }
